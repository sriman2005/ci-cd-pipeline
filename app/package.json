{
  "name": "ci-cd-app",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "lint:security": "eslint . --ext .js --config .eslintrc.json",
    "audit:security": "npm audit --audit-level=moderate",
    "test": "echo 'No tests configured' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-plugin-security": "^2.1.0"
  }
}
